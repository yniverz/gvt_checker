{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "gvt: setup venv",
            "type": "shell",
            "command": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
            "problemMatcher": []
        },
        {
            "label": "gvt: docker build",
            "type": "shell",
            "command": "docker compose build",
            "problemMatcher": []
        },
        {
            "label": "gvt: docker up",
            "type": "shell",
            "command": "docker compose up -d && docker compose logs -f",
            "problemMatcher": []
        }
    ]
}
